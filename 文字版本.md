## 注意 Warning
本文档暂时只适用于 iOS 7.0 - iOS 10.3.3  
文档只是对 semaphorin.sh 进行翻译，尚未进行优化处理  
文档尚未制作完成，请耐心等待  

## 你要准备的 What you need to prepare
1. iOS 设备 型号 (e.g. iPad5,3) $replace
2. iOS 设备 正在运行的 iOS 版本 (e.g. 15.8.4) $r
3. iOS 设备 将要降级的 iOS 版本 (e.g 9.3) $version
4. 一条 USB-A 转 Lightning 的数据线
5. 安装 Python, 并安装pyimg4

## 你要知道的 What you need to know
1. IVKey 获取地址 https://theapplewiki.com/wiki/Firmware_Keys
2. 固件 获取地址 https://ipsw.me

## 你要做的 What you should do  
**文件准备**  
***iOS 7 - 8***  
制作 8.4.1 的 ramdisk  
***iOS 9.x***  
如果你的设备型号是 iPhone6,x iPhone7,x  iPad4,x iPad5,x iPod7,x  
制作 9.3 的 ramdisk  
否则制作 11.4 的 ramdisk  
***iOS 10.0.x - 10.2.x***   
如果你的设备型号是 iPhone 8,x  
制作 11.1 和 11.4 的 ramdisk  
否则制作 10.3.3 和 11.4 的 ramdisk  
***iOS 10.3.x - 14.x***   
制作 14.3 的 ramdisk  

### 制作 ramdisk  
***本地固件提取法***  
1. 下载 所需ramdisk版本的 ipsw
2. 生成 IM4M 文件  
	若你的 CPU ID 为 0x7001  
	则使用0x7001.shsh文件生成  
	输入  
	img4tool -e -s 0x7001.shsh -m IM4M  
2. 提取	 
	iBSS.xxx.RELEASE.im4p  
	iBEC.xxx.RELEASE.im4p  
	kernelcache.release.xxx  
	DeviceTree.xxxap.im4p  
	xxx-xxxxx-xxx.dmg (选小包,不选大包,是restore ramdisk)  
	trustcache.img4 (Ramdisk目标版本为12.x-14.x 所需)  
（注：若目标版本为 iOS 10.x，如上文，iBSS和iBEC 请下载11.1或10.3.3版本，其余文件下载11.4版本）  
3. 解密部分提取文件  
	解密命令参考如下  
	如果你需要解密的是iBSS.j81.RELEASE.im4p  
	则输入  
	img4 -i iBSS.j81.RELEASE.im4p -o iBSS.dec -k iv值key值  
	其中iv值和key值是连在一起的，不要加空格  
	剩余文件同理  
	最后得出的文件是  
	iBSS.dec  
	iBEC.dec  
	kernelcache.dec  
	DeviceTree.dec  
	RestoreRamdisk.dmg  
	***kernelcache的特殊处理***  
	img4 -i kernelcache.release.xxx -o kcache.raw -k iv值key值  
	img4 -i kernelcache.release.xxx -o kernelcache.dec -k iv值key值
4. 调整 RestoreRamdisk.dmg 镜像大小  
	***iOS 7.x - 8.x ramdisk***  
	hdiutil resize -size 60M RestoreRamDisk.dmg  
	***iOS 9.x ramdisk***  
	hdiutil resize -size 80M RestoreRamDisk.dmg  
	***iOS 10.x - 15.x ramdisk***  
	hdiutil resize -size 120M RestoreRamDisk.dmg  
	***iOS 16.x - 17.x ramdisk***  
	hdiutil resize -size 210M RestoreRamDisk.dmg
5. 挂载 RestoreRamdisk.dmg  
	hdiutil attach -mountpoint /tmp/ramdisk RestoreRamDisk.dmg  
	sudo diskutil enableOwnership /tmp/ramdisk  
6. 注入 SSH 工具到 ramdisk  
	gzip -d ssh.tar.gz  
    sudo gnutar -xvf ssh.tar -C /tmp/ramdisk  
	sudo gnutar -xvf libcharset.1.dylib_libiconv.2.dylib.tar -C /tmp/ramdisk/usr/lib (ramdisk版本iOS 7.x - 11.x 操作)  
	sudo gnutar -xvf libresolv.9.dylib.tar -C /tmp/ramdisk/usr/lib (ramdisk版本为iOS 7.x - 13.3.x 操作)  
	sudo gnutar -xvf gpt.txt_hfs_dualboot.tar -C /tmp/ramdisk  
    sudo gnutar -xvf gpt.txt.tar -C /tmp/ramdisk  
	sudo gnutar -xvf fixup_update_partition.tar -C /tmp/ramdisk  
	hdiutil detach /tmp/ramdisk   
7. 重新打包 ramdisk  
	***ramdisk版本为iOS 7.x - 9.x 的处理方式***  
	img4tool -c ramdisk.im4p -t rdsk RestoreRamDisk.dmg  
	img4tool -c ramdisk.img4 -p ramdisk.im4p -m IM4M  
	***ramdisk版本为iOS 10.x - 15.x 的处理方式***  
	 hdiutil resize -sectors min RestoreRamDisk.dmg  
	 img4 -i RestoreRamDisk.dmg -o ramdisk.img4 -M IM4M -A -T rdsk  
	***ramdisk版本为iOS 16.x - 17.x 的处理方式***  
	 hdiutil resize -sectors min RestoreRamDisk1.dmg  
	 img4 -i RestoreRamDisk1.dmg -o ramdisk.img4 -M IM4M -A -T rdsk      
8. 修补 iBSS 等相关文件  
	***ramdisk版本为iOS 9.x 的 5s，6，Air，Air 2，Touch 6设备***  
	kairos iBSS.dec iBSS.patched  
	kairos iBEC.dec iBEC.patched -b "rd=md0 debug=0x2014e amfi=0xff cs_enforcement_disable=1 $boot_args wdt=-1 -restore" -n  
	***ramdis版本为iOS9.x 的其他设备***  
	kairos iBSS.dec iBSS.patched
	kairos iBEC.dec iBEC.patched -b "amfi=0xff cs_enforcement_disable=1 $boot_args rd=md0 nand-enable-reformat=1 -progress" -n  
	***ramdisk版本为iOS7.x - 8.x***  
	ipatcher iBSS.dec iBSS.patched  
	ipatcher iBEC.dec iBEC.patched -b "amfi=0xff cs_enforcement_disable=1 $boot_args rd=md0 nand-enable-reformat=1 -progress"  
	***ramdisk版本为iOS10.x - 17.x 的 A7、A8、A8X设备***  
	iBoot64Patcher iBSS.dec iBSS.patched
	iBoot64Patcher iBEC.dec iBEC.patched -b "rd=md0 debug=0x2014e $boot_args wdt=-1 -restore"  
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
    img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
	KPlooshFinder kcache.raw kcache2.patched (ramdisk版本为10.3.x 执行此条不执行下一条，否则执行下一条不执行本条)
	Kernel64Patcher2 kcache.raw kcache2.patched -a  
	kerneldiff kcache.raw kcache2.patched kc.bpatch  
	img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn -P kc.bpatch  
	img4 -i kernelcache.dec -o kernelcache -M IM4M -T krnl -P kc.bpatch  
	img4 -i trustcache.im4p -o trustcache.img4 -M IM4M -T rtsc (ramdisk版本为iOS 12.x - 17.x 执行)  
	img4 -i devicetree.dec -o devicetree.img4 -M IM4M -T rdtr  
	***ramdisk版本为iOS10.x - 17.x 的 其他设备***  
	iBoot64Patcher iBSS.dec iBSS.patched
	iBoot64Patcher iBEC.dec iBEC.patched -b "amfi=0xff cs_enforcement_disable=1 $boot_args rd=md0 nand-enable-reformat=1 amfi_get_out_of_my_way=1 -restore -progress" -n  
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
    img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
	KPlooshFinder kcache.raw kcache2.patched (ramdisk版本为10.3.x 执行此条不执行下一条，否则执行下一条不执行本条)
	Kernel64Patcher2 kcache.raw kcache2.patched -a  
	kerneldiff kcache.raw kcache2.patched kc.bpatch  
	img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn -P kc.bpatch  
	img4 -i kernelcache.dec -o kernelcache -M IM4M -T krnl -P kc.bpatch  
	img4 -i trustcache.im4p -o trustcache.img4 -M IM4M -T rtsc (ramdisk版本为iOS 12.x - 17.x 执行)  
	img4 -i devicetree.dec -o devicetree.img4 -M IM4M -T rdtr  

9. 重新打包 iBSS 等相关文件 (iOS 10.x+ 版本无需操作，上一步已经打包成功)  
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
	img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
	img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn  
	img4 -i devicetree.dec -o devicetree.img4 -A -M IM4M -T rdtr  


### 制作 boot files  
***本地固件制作法***  
1. 下载 所需降级版本的 ipsw  
2. 生成 IM4M 文件  
	若你的 CPU ID 为 0x7001  
	则使用0x7001.shsh文件生成  
	输入  
	img4tool -e -s 0x7001.shsh -m IM4M  
3. 处理 iBSS iBEC 文件  
	(若你正在降级 iOS 10 , 6s系列请使用11.1的iBSS和iBEC, 其他系列使用 10.3.3 的iBSS和iBEC)  
	img4 -i iBSS.xxx.RELEASE.im4p -o iBSS.dec -k IV值Key值  
	img4 -i iBEC.xxx.RELEASE.im4p -o iBEC.dec -k IV值Key值  
4. 处理 kernelcache 和 DeviceTree  
	***iOS 7.x - 9.x***  
	img4 -i kernelcache.release.xxx -o kcache.raw -k IV值Key值  
	img4 -i kernelcache.release.xxx -o kernelcache.dec -k IV值Key值 -D  
	img4 -i DeviceTree.xxxap.im4p -o DeviceTree.dec -k IV值Key值  
	***iOS 10 - 14***
	img4 -i kernelcache.release.xxx -o kcache.raw -k  
	img4 -i kernelcache.release.xxx -o kernelcache.dec -k -D  
	pyimg4 im4p extract -i kernelcache.release.xxx -o kcache.raw.pyimg4 --iv IV值 --key Key值 --extra kpp.bin  
	mv DeviceTree.xxxap.im4p DeviceTree.dec  
5. 处理 RestoreRamdisk  
	***iOS7.x - 9.x***
	img4 -i xxx-xxxxx-xxx.dmg -o RestoreRamDisk.dmg -k IV值Key值  
	***iOS 10 - 14***
	img4 -i xxx-xxxxx-xxx.dmg -o RestoreRamDisk.dmg  
6. (iOS 12+) 处理 Trustcache  
	mv xxxx.trustcache trustcache.im4p  
7. 开始 patch iBSS 等文件  
	***设备现处版本为 iOS 15及以下***  
	**iOS7**  
	ipatcher iBSS.dec iBSS.patched    
	ipatcher iBEC.dec iBEC.patched -b "$boot_args rd=disk0s1s1 amfi=0xff cs_enforcement_disable=1 keepsyms=1 debug=0x2014e wdt=-1 PE_i_can_has_debugger=1 amfi_get_out_of_my_way=0x1 amfi_unrestrict_task_for_pid=0x0"  
	**iOS8**  
	ipatcher iBSS.dec iBSS.patched  
	ipatcher iBEC.dec iBEC.patched -b "$boot_args rd=disk0s1s1 amfi=0xff cs_enforcement_disable=1 keepsyms=1 debug=0x2014e PE_i_can_has_debugger=1"  
	**iOS9**  
	kairos iBSS.dec iBSS.patched  
	kairos iBEC.dec iBEC.patched -b "$boot_args rd=disk0s1s1 amfi=0xff cs_enforcement_disable=1 keepsyms=1 debug=0x2014e PE_i_can_has_debugger=1 amfi_get_out_of_my_way=1 amfi_allow_any_signature=1" -n  
	**iOS10+**  
	iBoot64Patcher iBSS.dec iBSS.patched  
	iBoot64Patcher iBEC.dec iBEC.patched -b "$boot_args rd=disk0s1s8 amfi=0xff cs_enforcement_disable=1 keepsyms=1 debug=0x100 PE_i_can_has_debugger=1 amfi_get_out_of_my_way=1 amfi_allow_any_signature=1" -n (HFS文件系统版本)  
	iBoot64Patcher iBEC.dec iBEC.patched -b "$boot_args rd=disk0s1s1 amfi=0xff cs_enforcement_disable=1 keepsyms=1 debug=0x2014e PE_i_can_has_debugger=1 amfi_get_out_of_my_way=1 amfi_allow_any_signature=1" -n (APFS文件系统版本)  
8. 继续 修补  
	***iOS 7***  
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
    img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
    Kernel64Patcher kcache.raw kcache.patched -u 7 -m 7 -e 7 -f 7 -k  
    kerneldiff kcache.raw kcache.patched kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn -P kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache -M IM4M -T krnl -P kc.bpatch  
    dtree_patcher DeviceTree.dec DeviceTree.patched -n  
    img4 -i DeviceTree.patched -o devicetree.img4 -A -M IM4M -T rdtr  
	***iOS 8***
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
	img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
	Kernel64Patcher kcache.raw kcache.patched -u 8 -t -p -e 8 -f 8 -a -m 8 -g -s -d  
	kerneldiff kcache.raw kcache.patched kc.bpatch  
	img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn -P kc.bpatch  
	img4 -i kernelcache.dec -o kernelcache -M IM4M -T krnl -P kc.bpatch  
	dtree_patcher DeviceTree.dec DeviceTree.patched -n  
	img4 -i DeviceTree.patched -o devicetree.img4 -A -M IM4M -T rdtr  
	***iOS 9***  
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
    img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
    Kernel64Patcher kcache.raw kcache.patched -u 9 -f 9 -k  
    kerneldiff kcache.raw kcache.patched kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn -P kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache -M IM4M -T krnl -P kc.bpatch  
    dtree_patcher DeviceTree.dec DeviceTree.patched -n  
    img4 -i DeviceTree.patched -o devicetree.img4 -A -M IM4M -T rdtr  
	***iOS 10.0.x - 10.2.x***  
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
    img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
	KPlooshFinder kcache.raw kcache.patched  
    Kernel64Patcher kcache.patched kcache2.patched -u 10 -a -f 10 -q  
    kerneldiff kcache.raw kcache2.patched kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn -P kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache -M IM4M -T krnl -P kc.bpatch  
	img4tool -e -o devicetree.out DeviceTree.dec  
    dtree_patcher devicetree.out DeviceTree.patched -n  
    img4 -i DeviceTree.patched -o devicetree.img4 -A -M IM4M -T rdtr  
	***iOS 10.3.x***  
	img4 -i iBSS.patched -o iBSS.img4 -M IM4M -A -T ibss  
    img4 -i iBEC.patched -o iBEC.img4 -M IM4M -A -T ibec  
	KPlooshFinder kcache.raw kcache.patched  
    Kernel64Patcher kcache.patched kcache2.patched -u 10 -a -f 10 -q  
    kerneldiff kcache.raw kcache2.patched kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache.img4 -M IM4M -T rkrn -P kc.bpatch  
    img4 -i kernelcache.dec -o kernelcache -M IM4M -T krnl -P kc.bpatch  
	img4tool -e -o devicetree.out DeviceTree.dec  
    dtree_patcher devicetree.out DeviceTree.patched -n  
    img4 -i DeviceTree.patched -o devicetree.img4 -A -M IM4M -T rdtr  
	***iOS 11.x***  
	***iOS 12.x***  
	***iOS 13.x***  
	***iOS 14.x***  





### 制作 root file system  
***本地固件制作法***  
***iOS 7.x - 10.2.x***  
1. 下载 所需降级版本的 ipsw  
2. 提取  
	xxx-xxxxx-xxx.dmg (最大的那个包)  
3. 解密  
	dmg extract xxx-xxxxx-xxx.dmg OS.dmg -k IV值Key值  
4. 将.dmg改为.tar  
	dmg build OS.dmg rw.dmg  
	hdiutil attach -mountpoint /tmp/ios rw.dmg  
	sudo diskutil enableOwnership /tmp/ios  
	sudo gnutar -cvf OS.tar -C /tmp/ios  
	hdiutil detach /tmp/ios  
5. 意义不明 (不知道有什么作用)  
	若设备为A7设备 则执行  
	irecovery -f /dev/null

***iOS 10.3.x - 14.x***
1. 下载 所需降级版本的 ipsw  
2. 提取  
	xxx-xxxxx-xxx.dmg (最大的那个包)  
3. 生成 OS.dmg  
 	asr -source xxx-xxxxx-xxx.dmg -target OS.dmg --embed -erase -noprompt --chunkchecksum --puppetstrings  
4. 意义不明 (不知道有什么作用)  
	若设备为A7设备 则执行  
	irecovery -f /dev/null  




### 备份激活文件
1. 越狱设备，安装AFC2 / Filza 这样能提取文件的插件
2. 分别提取  
	apticket.der  
	sep-firmware.img4  
	FUD (folder)  
	Baseband (folder)  
	firmware (folder)  
	local (folder)  
	keybags (folder)  
	wireless (folder)  
	com.apple.factorydata  
	IC-Info.sisv  
	com.apple.commcenter.device_specific_nobackup.plist  
	data_ark.plist  
	activation_records  

路径分别为  
	/var/root/Library/Lockdown/data_ark.plist  
	/var/wireless/Library/Preferences/com.apple.commcenter.device_specific_nobackup.plist  
	/var/mobile/Library/FairPlay/iTunes_Control/iTunes/IC-Info.sisv  
	/System/Library/Caches/com.apple.factorydata  
	/var/wireless  
	/var/keybags  
	/usr/local  
	/usr/standalone/firmware  
	/usr/local/standalone/firmware/Baseband  
	/usr/standalone/firmware/FUD  
	/usr/standalone/firmware/sep-firmware.img4  
	/System/Library/Caches/apticket.der  
	activation_records 的 麻烦各位在/var/containers/Data/System下的这么多的文件夹好好找找 在某一个文件夹的/Library下  

### 刷写



### 清理原有分区 Wipe the partition  
root@localhost lwvm init

### Activation

### 引导  
1.  A7(X)设备执行   
	ipwnder -p    
	gaster reset  
其他设备执行  
	gaster pwn  
	gaster reset  
2. 输入  
	irecovery -f iBSS.img4  
	irecovery -f iBEC.img4  
	（若为A10(X)设备，在这一条命令输入完成后，输入irecovery -c go）  
	irecovery -f devicetree.img4  
	irecovery -c devicetree  
	（若存在trustcache，则输入irecovery -f trustcache.img4  
							irecovery -c firmware） 
	irecovery -f kernelcache.img4  
	irecovery -c bootx  





## 型号对照表
iPhone 6,x 为 iPhone 5s 系列

iPhone 7,x 为 iPhone 6 系列

iPhone 8,x 为 iPhone 6s 系列

iPhone 9,x 为 iPhone 7 系列

iPhone 10,x 为 iPhone X 系列

iPad 4,x 为 iPad Air 1st 系列

iPad 5,x 为 iPad Air 2 系列

iPad 7,x 为 iPad Pro 1st 系列

## CPUID 对照表
0x8960 为 Apple A7

0x7000 为 Apple A8

0x7001 为 Apple A8X

0x8000 为 Apple A9 三星版本

0x8001 为 Apple A9X

0x8003 为 Apple A9 台积电版本

0x8010 为 Apple A10

0x8011 为 Apple A10X

0x8015 为 Apple A11